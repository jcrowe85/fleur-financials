# Non-secret, environment-agnostic defaults. Safe to commit.
# Secrets live in .env.local (gitignored), which overrides these.

# Business timezone for sales-day boundaries (Amazon reports use Pacific Time).
# See src/lib/metrics.ts. Override per-environment in .env.local if needed.
BUSINESS_TIMEZONE=America/Los_Angeles
